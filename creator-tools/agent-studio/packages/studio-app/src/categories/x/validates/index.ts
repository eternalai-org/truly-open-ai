import CREATE_X_VALIDATORS from "./create";
import UPDATE_X_VALIDATORS from "./update";

const X_VALIDATES = {
  create: CREATE_X_VALIDATORS,
  update: UPDATE_X_VALIDATORS,
};

export default X_VALIDATES;
